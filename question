Create queue with the following operations:
1) Initialize the queue
2) Check whether the queue is empty or not
3) Add an element into the queue
4) Remove an element from the queue
5) Display the entire queue